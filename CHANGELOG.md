# Changelog

## [v1.0.0] - 2026-02-10

# Release Notes
- generated_utc: `2026-02-10T19:32:18.770806+00:00`
- range: `root..HEAD`
- commit_count: `95`
## Documentation
- Add multilingual adapter support matrix, usage examples, and lexical limitations (`a849144`)
## Tests
- Add multilingual adapter selection coverage for repo.outline (`9ccf376`)
- Add multilingual adapter golden snapshots and outline determinism integration (`ad75077`)
## Other
- Initial Commit - Project specifications and gitignore (`960d4b1`)
- TODO.md - Initial creation (`9c724c1`)
- Step1: scaffold repo_mcp package, tooling config, entrypoint, and smoke tests (`fc60a72`)
- Step2: add stdio request routing, deterministic tool registry, error envelopes, and tool stubs (`e10bcad`)
- Step3: enforce repo-root path sandboxing and blocked response semantics (`7ba5eff`)
- Step4: enforce denylist and response/file/search limits with blocked-policy errors (`d6bafb1`)
- Step5: add structured JSONL audit logging and repo.audit_log tool (`b5717b5`)
- Step6: add typed config loading/merge order and expose effective config in repo.status (`5a86003`)
- Step7: add deterministic file discovery and incremental index delta bookkeeping (`2d8d0f9`)
- Step8: add deterministic line chunking with stable chunk ids and chunking status summary (`5cdb087`)
- Step10: add persistent incremental index store with schema v1 and refresh/status wiring (`d9b65ba`)
- Step11: add deterministic BM25 search with stable ranking, filters, and repo.search wiring (`1286fc9`)
- Step12: add adapter protocol/registry and lexical fallback adapter (`1220ee5`)
- Step13: add python ast outline adapter and repo.outline tool wiring (`49e2d9e`)
- Step15: add deterministic context bundler engine with budgets, dedupe/ranking, citations, and audit metadata (`c2a6836`)
- Step16: add repo.build_context_bundle tool with deterministic exports and warning-safe artifact persistence (`e6516f4`)
- Step17: harden core tool contracts and add deterministic tool matrix coverage (`ca630f9`)
- Step19: enforce capped limit override policy and denylist non-relaxability (`7cd214a`)
- Step20: add stdio subprocess e2e workflow tests and no-network integration guard (`a913e60`)
- Step21: add cross-platform normalization tests and stabilize path_prefix normalization (`1da796d`)
- Step22: add CI workflow for lint/type/test/build and artifact install smoke tests (`58af995`)
- Step24: add tag-driven release workflow with GitHub artifacts and guarded optional PyPI publish (`847932f`)
- Implemented production-grade documentation based on current code and tool behavior. (`8236222`)
- Validation Workflow shell script. (`429ffb6`)
- Added Repo link (`a20b2fa`)
- Small format fix. (`7d8c942`)
- Add dependencies for dev and release. (`5d1d938`)
- Added TODO for other languages. (`3638693`)
- Adapters: add shared outline contract validation and deterministic ordering (`29b49bf`)
- Adapters: add deterministic shared lexical scanner utilities and tests (`617f9b4`)
- Adapters: add deterministic lexical TypeScript/JavaScript outline support (`a34186d`)
- Adapters: add deterministic lexical Java outline adapter (`8e4248a`)
- Adapters: add deterministic lexical Go outline adapter (`7b6792d`)
- Adapters: add deterministic lexical Rust outline adapter (`fbf783b`)
- Adapters: add deterministic lexical C++ outline adapter (`dac87ec`)
- Adapters: add deterministic lexical C# outline adapter (`33fd6d3`)
- Bundler: consume adapter outlines for symbol-aligned non-python selections with safe fallback (`7488238`)
- Decision: no-tree-sitters. Added adr to explain rational. (`4019d6a`)
- Decision: disallow external toolchain integration (`24166e6`)
- Decision: outline only no language aware chunking (`06f707d`)
- Updated `SPEC.md` with concrete v2 outline contract and rollout anchors. (`c51406a`)
- Implement Phase A for v2 outline semantics per SPEC 10.3: Python adapter nested/conditional extraction plus tests only. (`ae2cd4a`)
- Implemented Phase B: update shared contract and repo.outline response fields for optional v2 metadata, with tests. (`c0dff31`)
- Implemented Phase C without changing non-Python extraction logic. (`76e8930`)
- Updated demo script and documentation. (`28ff505`)
- Added License (`e81a9b9`)
- Added Code of Conduct (`65ae1d7`)
- Added Contributing.md (`69cf6a3`)
- Updated Security Policy (`90c3969`)
- Added basic templates (`9f1416e`)
- Archived finished todo list (`17f5b3b`)
- Updated TODO for v2.5 (`46e9d8c`)
- Implemented exactly the two documentation tasks you asked for, with no implementation code changes. (`8464b59`)
- Implemented V2.5-REF-003 with Python AST-based reference extraction and deterministic ordering. (`0413803`)
- Implemented V2.5-RANK-001 in SPEC.md and marked it complete in TODO.md. (`a07e35a`)
- Implemented V2.5-RANK-002 by adding deterministic why_selected explanations to bundle selections. (`ae08287`)
- Implemented V2.5-TEST-001 with golden regression coverage for references and ranking stability. (`f55ce06`)
- Implemented V2.5-REF-004 with deterministic lexical cross-file reference fallback for non-Python adapters. (`0410b17`)
- Implemented V2.5-REF-005 with a new repo.references tool and supporting limits/config plumbing. (`55d5d8c`)
- Implemented V2.5-RANK-003 with deterministic symbol/usage-proximity ranking. (`bb679be`)
- Implemented V2.5-OBS-001 by adding bounded, deterministic bundle-ranking audit fields for explainability debugging. (`196df0a`)
- Completed V2.5-DOC-001 and updated both docs with v2.5 examples aligned to current behavior. (`9da968d`)
- Added Noise file removal for search tools (`8c371cc`)
- Feat(v2.5): align repo.references discovery with index filters and add deterministic noise exclusions docs(config): add copy/paste quick profiles for Python, Node/TS, and polyglot repos (`f5cbe9a`)
- Added(repo_mcp.toml) to root dir of project so project can reference itself in testing. (`f277045`)
- Docs/perf: improve v2.5 discovery defaults and add LLM config/performance guidance (`bb4e8ac`)
- Benchmarking (`623728a`)
- Implemented PERF-002 with medium/large benchmark scenarios, stable protocol metadata, and artifact retention. (`e2bc0af`)
- Implemented PERF-003 end-to-end: targeted profiling for repo.references candidate discovery and adapter resolution is now available and integrated into benchmark artifacts. (`e36f0c8`)
- Implemented PERF-004: targeted profiling for bundler dedupe/ranking/budget-enforcement paths is now in place and integrated into benchmark artifacts. (`accffb9`)
- Implemented PERF-005 and aligned it with the existing profiling contract. (`93de5f6`)
- Benchmarking. Part II (`dc9ba8f`)
- Performance Round 1 (`a80ee37`)
- Performance II (`a5d22df`)
- Performance III (`06ed731`)
- Revert (`86e25c4`)
- Performance III (part duex) (`940549e`)
- Performance IV (`1473342`)
- Performance V (`acc8f4b`)
- Feat(perf): reduce bundler/reference/search-index overhead for deterministic workflow runs (`f80936e`)
- Next Tasks Updates (`6df2892`)
- Added quality golden fixtures for difficult prompts to validate context relevance stability (not just schema/order). (`084d142`)
- Implemented USE-OBS-001 with deterministic, bounded why_not_selected explainability and updated tests/validator. (`c848c4b`)
- Addressed. mypy errors are fixed and now clean across src. (`809c754`)
- Add recommended setup presets and tuning guidance to `docs/CONFIG.md` and `examples/repo_mcp.toml`. (`73dac56`)
- Added local/CI helper command, Updated perf playbook with explicit baseline workflow (`58e5cbe`)
- Ruff format fix. (`fd3b053`)
- Testing perf guard rails. (`d6e522a`)
- Fixed run_perf_guardrails for CI (`22b5dad`)
- Doc updates. (`213710f`)
- `DX-001` Add maintainership automation for changelog/release notes. (`1bd92ff`)
- `DX-002` Add issue labels/triage docs for single-maintainer workflow. (`a90ae58`)

